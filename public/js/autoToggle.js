// Toggles the menu option button if the menu needs to be opened
// looks for the toggleMe id, should be unique and only one per page
const toggleMe = document.getElementById('toggleMe') || null;

if (toggleMe) {
    toggleMe.click();
}
